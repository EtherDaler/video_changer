# ──────────────────────────────────────────────────────────────
# Core ML
# NOTE: Install PyTorch FIRST from https://pytorch.org/get-started
# BEFORE running pip install -r requirements.txt
#
#   RTX 5000-й серии (Blackwell, sm_120) — CUDA 12.8:
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
#   CUDA (RTX 3000/4000-й, NVIDIA GPU):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#   macOS (Apple Silicon / Intel):
#     pip install torch torchvision torchaudio
#
#   CPU only:
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# ──────────────────────────────────────────────────────────────

# Diffusion models
# ≥0.33.0 required for IP-Adapter SDXL support (pipe.load_ip_adapter)
diffusers>=0.33.0
# groundingdino-py is incompatible with transformers>=4.48 (BertModel.get_head_mask issue)
transformers>=4.40.0,<4.48.0
accelerate>=0.30.0
safetensors>=0.4.0

# ControlNet helpers (depth, canny, etc.)
controlnet_aux>=0.0.9
timm>=0.9.0
einops>=0.7.0

# SAM2 — install from source
-e git+https://github.com/facebookresearch/sam2.git#egg=SAM_2

# Grounding DINO — object detection by text
groundingdino-py>=0.4.0

# Computer vision
opencv-python>=4.8.0
scikit-image>=0.22.0
scipy>=1.12.0

# Image / video I/O
Pillow>=10.0.0
imageio>=2.33.0
imageio-ffmpeg>=0.5.0

# Numerics
numpy>=1.26.0

# HuggingFace utils
huggingface_hub>=0.23.0

# Misc
python-dotenv>=1.0.0
tqdm>=4.66.0
requests>=2.31.0
replicate>=0.26.0
PyYAML>=6.0
omegaconf>=2.3.0        # needed for RAFT
invisible-watermark>=0.2.0

# Optional — memory-efficient attention on CUDA (skip on macOS / CPU)
# xformers>=0.0.26

# Optional — needed by torch.compile (--torch-compile flag) on Linux/Windows
# Usually ships with PyTorch; install separately only if torch.compile fails:
# pip install triton
